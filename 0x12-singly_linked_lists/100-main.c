#include <stdio.h>

/**
 * startup_message - Prints a startup message.
 */
void startup_message(void);

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	startup_message();
	printf("(A tortoise, having pretty good sense of a hare's nature, challenges one to a race.)\n");
	return (0);
}
