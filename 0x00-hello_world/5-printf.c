#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * This program prints a specific message using the printf function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
